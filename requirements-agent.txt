# LiveKit Agents Worker Dependencies
# Install with: pip install -r requirements-agent.txt
# Or using uv: uv add "livekit-agents[openai,deepgram,cartesia,silero]~=1.3"

# Core LiveKit Agents framework
livekit-agents>=1.3.0,<2.0.0

# STT Plugin - Deepgram
livekit-plugins-deepgram>=1.0.0

# LLM Plugin - OpenAI
livekit-plugins-openai>=1.0.0

# TTS Plugin - Cartesia
livekit-plugins-cartesia>=1.0.0

# VAD Plugin - Silero (Voice Activity Detection)
livekit-plugins-silero>=1.0.0

# HTTP client for backend API calls
httpx>=0.27.0

# Environment variable loading
python-dotenv>=1.0.0
